{% extends "template.html.twig" %}
{% block styleSheet %}
    <link rel="stylesheet" type="text/css" href="../../estilos-paginas/cadastro-atleta.css"/>
{% endblock %}
{% block body %}
    <form class="form-horizontal-a" method="post" action="../rodada/editar.php?id={{rodada.id}}" style="width: 700px;"> 
        {{mensagem}}
        <input type="hidden" name="editar" value="1"/>
        <input type="hidden" name="id" value="{{rodada.id}}"/>
        <h3>Modificando a Rodada</h3>
        <div class="form-group-a">
            <label  class="col-sm-2 control-label">Numero</label>
            <div class="col-sm-10-a">
                <input name="numero" type="text" class="form-control" maxlength="60" value="{{rodada.numero}}">
            </div>
        </div> 
        <div class="form-group-b">
            <div class="col-sm-offset-2 col-sm-10-a">
                <button type="submit" class="btn btn-default">Alterar</button>
            </div>
        </div>
        <div class="col-sm-12-a col-md-12">
            <div class="form-group-b">
                <div class="col-sm-offset-2 col-sm-12-a">
                    <a href="gerenciar.php"> <button  Type="button" class="btn btn-default" >Voltar</button></a>
                </div>
            </div>
        </div>
    </form>
{% endblock %}